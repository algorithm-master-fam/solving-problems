```python
def solution(num):
	next_num = num
	while True:
		next_num += 1
		length = func_b(num)
		if length % 2:
			continue
		divisor = func_c(length)  
		front = next_num // divisor
		back = next_num % divisor

		front_sum = func_a(front)
		back_sum = func_a(back)
		if front_sum == back_sum:
			break
	return next_num - num
```
